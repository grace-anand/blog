---
import { formatDistance } from 'date-fns'

export interface Props {
  pubTimestamp: Date;
	className: string;
}
const { pubTimestamp, className } = Astro.props;
const publishedAt = formatDistance(new Date(pubTimestamp), new Date(), { addSuffix: true })
---

<div class={`flex items-center ${className}`}>
	<img
		src="https://avatars.githubusercontent.com/u/95082903?s=48&v=4"
		alt="anand"
		class="w-12 h-12 rounded-full mr-4"
	/>
	<div class="text-sm flex">
		<div class="text-gray-500 ml-1">@anand</div>
		<div class="text-gray-500 ml-1">•</div>
		<div class="text-gray-500 ml-1">{publishedAt}</div>
	</div>
</div>
