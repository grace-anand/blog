const prettyCodeOptions = {
	theme: 'one-dark-pro',
	tokensMap: {
		fn: 'entity.name.function',
		var: 'keyword',
		str: 'string',
		num: 'constant.numeric',
		cmt: 'comment',
	},
};

export default prettyCodeOptions;
